import Foundation

// NOTE: Do not commit real tokens to source control.
// Consider reading from environment, Keychain, or a configuration file for production.
enum Secrets {
    // Replace with your real Airtable API token (e.g., "patXXXXXXXX...")
    static let apiToken: String = "<#YourAirtableAPIToken#>"
}
